const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),vm=require('node:vm');
const s=fs.readFileSync(require('node:path').join(__dirname,'../app.js'),'utf8'),c={};vm.createContext(c);vm.runInContext(s.slice(s.indexOf('function initialDocumentPath'),s.indexOf('function restoreLastOpenFile')),c);
test('start document yields to valid last document and only matches root introduction',()=>{const paths=['Werkbank/00 - Begin hier.md','Werkbank/notitie.md','Werkbank/sub/Begin hier.md'];assert.equal(c.initialDocumentPath(paths,null,['Werkbank']),paths[0]);assert.equal(c.initialDocumentPath(paths,paths[1],['Werkbank']),paths[1]);assert.equal(c.initialDocumentPath([paths[2]],null,['Werkbank']),null);assert.equal(c.initialDocumentPath(['Werkbank/Begin hier.md'],null,['Werkbank']),'Werkbank/Begin hier.md');});
